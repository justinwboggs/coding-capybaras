# `/product/INTERFACES.md`

This file documents anything your product code exposes that another region might need to call. In most apps this stays short — `/product/` is the top of the dependency graph (it consumes `/platform/`, not the other way around).

> 📦 **Placeholder — filled in during Tranche 11b-2.**
>
> For now, the interfaces YOU call FROM `/product/` are documented in
> `/platform/INTERFACES.md`. That's the contract that matters most.
