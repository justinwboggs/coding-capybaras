# `/website/INTERFACES.md`

This file lists what `/website/` exposes to the rest of the repo — typically nothing, since `/website/` is leaves at the edge of the dependency graph. If you build a reusable marketing component that another region needs (rare), document it here.

> 📦 **Placeholder — filled in during Tranche 11b-2.**
>
> In most cases this stays a short file: marketing code is a consumer, not a
> provider. The interfaces YOU call from `/website/` are documented in
> `/platform/INTERFACES.md`.
